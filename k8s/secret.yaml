apiVersion: v1
kind: Secret
metadata:
  name: devops-ai-assistant-secret
  namespace: ai-platform
type: Opaque
stringData:
  # stringData lets you write plain text — K8s base64-encodes it automatically
  # In production: use Sealed Secrets or Vault, never commit real values
  OLLAMA_HOST: "http://host.minikube.internal:11434"
