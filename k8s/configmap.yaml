apiVersion: v1
kind: ConfigMap
metadata:
  name: devops-ai-assistant-config
  namespace: ai-platform
data:
  # Non-sensitive config only — secrets go in secret.yaml
  APP_ENV: "production"
  LOG_LEVEL: "INFO"
